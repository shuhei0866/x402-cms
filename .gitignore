# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/
build/
dist/

# Env
.env
.env.local
.env.*.local

# GCP credentials (Phase 1+ for Firestore / Cloud Run service accounts)
gcp-key*.json
service-account*.json
*-credentials.json
.gcloud/

# Data (cache, regeneratable; views mirror the vault source of truth)
data/source/
data/views/
data/cache.db

# Build / deploy artifacts
build/
*.log

# Private — curator-specific judgement layer.
# Public examples (*.example.md) are kept; everything else under prompts/ is private.
config/tracked_handles.yaml
config/importance_rules.yaml
prompts/*
!prompts/*.example.md

# Pre-commit hook private layer (Layer 1 patterns live here, gitignored)
.git/hooks/personal-patterns.local.txt

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
